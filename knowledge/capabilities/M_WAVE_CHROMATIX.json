{
  "schema_version": 1,
  "component": "M_WAVE_CHROMATIX",
  "probe": "benchmarks/probes/precision/chromatix_capability.py",
  "probe_tag": "pre-rewrite-2026-08-30",
  "evidence": "chromatix 0.6.0 @ d24bdf0: ScalarField.__init__ is jnp.asarray(u, dtype=jnp.complex64) unconditionally, and Field.build(complex128) returns complex64 even under jax_enable_x64=True, so there is no complex128 path at any device; asm_propagate returns complex64 on cuda:0 (agent_solver_gpu, jax 0.6.2 backend gpu)",
  "notes": "complex128 is ingestible and silently truncated by Chromatix itself, which is why it is declared lossy rather than accepted: the loss then happens at a boundary that records it, instead of inside ScalarField where nothing measures it. A requested device must never be reported as an actual one -- a process-global JAX platform pin produces a successful complex64 run on the host while the caller asked for CUDA, with no error raised.",
  "devices": [
    "cpu",
    "cuda"
  ],
  "precisions": [
    "fp32"
  ],
  "minimum_compute_precision": "fp32",
  "accepted_input_dtypes": [
    "complex64"
  ],
  "native_compute_dtypes": [
    "complex64"
  ],
  "output_dtypes": [
    "complex64"
  ],
  "lossy_input_dtypes": [
    "complex128"
  ],
  "device_namespaces": {
    "cpu": [
      "jax"
    ],
    "cuda": [
      "jax"
    ]
  }
}
