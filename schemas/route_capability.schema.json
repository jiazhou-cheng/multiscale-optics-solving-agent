{
  "$defs": {
    "Handover": {
      "additionalProperties": false,
      "description": "One adjacent pair, and whether the artifact can cross between them.",
      "properties": {
        "consumer": {
          "title": "Consumer",
          "type": "string"
        },
        "exact_dtypes": {
          "items": {
            "type": "string"
          },
          "title": "Exact Dtypes",
          "type": "array"
        },
        "lossy": {
          "default": false,
          "title": "Lossy",
          "type": "boolean"
        },
        "lossy_dtypes": {
          "items": {
            "type": "string"
          },
          "title": "Lossy Dtypes",
          "type": "array"
        },
        "possible": {
          "default": true,
          "title": "Possible",
          "type": "boolean"
        },
        "producer": {
          "title": "Producer",
          "type": "string"
        }
      },
      "required": [
        "producer",
        "consumer"
      ],
      "title": "Handover",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "What an ordered route can execute, at two different levels.\n\nThe two are genuinely different questions and conflating them produces a\nfalse negative on the repository's own flagship route:\n\n``feasible``\n    can the artifact cross every adjacent pair? A ray bundle in float64\n    handing over to a wave model in complex64 is a *representation change*,\n    so the dtypes differ by construction and the route runs anyway.\n``uniform_precision_available``\n    is there one precision at which the WHOLE route computes? Often not,\n    and that is the R5 statement: Chromatix computes only in complex64,\n    C_PATCH_WFT only in complex128, so a route through both has no single\n    precision -- reported here, before execution, with the pair named.\n\nAn earlier draft intersected native compute dtypes and called the result\nfeasibility, which declared ``M_RAY_OPTILAND -> C_RAY_TO_WAVE ->\nM_WAVE_CHROMATIX`` infeasible. That route executes; it is\n``examples/graphs/ray_to_wave.yaml``.",
  "properties": {
    "blocking_pair": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Blocking Pair"
    },
    "devices": {
      "items": {
        "type": "string"
      },
      "title": "Devices",
      "type": "array"
    },
    "feasible": {
      "title": "Feasible",
      "type": "boolean"
    },
    "handovers": {
      "items": {
        "$ref": "#/$defs/Handover"
      },
      "title": "Handovers",
      "type": "array"
    },
    "lossy_handovers": {
      "items": {
        "type": "string"
      },
      "title": "Lossy Handovers",
      "type": "array"
    },
    "reason": {
      "default": "",
      "title": "Reason",
      "type": "string"
    },
    "route": {
      "items": {
        "type": "string"
      },
      "title": "Route",
      "type": "array"
    },
    "uniform_compute_dtypes": {
      "items": {
        "type": "string"
      },
      "title": "Uniform Compute Dtypes",
      "type": "array"
    },
    "uniform_precision_available": {
      "default": true,
      "title": "Uniform Precision Available",
      "type": "boolean"
    }
  },
  "required": [
    "route",
    "feasible"
  ],
  "title": "RouteCapability",
  "type": "object"
}
