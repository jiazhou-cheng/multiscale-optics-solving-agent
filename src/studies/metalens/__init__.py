"""The metalens bridge study: a wave -> ray -> wave round trip at scale.

`candidate.py` builds the candidate device, `controller.py` drives the bounded
memory sweep, and `oracle.py` is the independent check the sweep is measured
against.
"""
