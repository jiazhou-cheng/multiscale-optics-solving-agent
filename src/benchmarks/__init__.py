"""Executable benchmarks that compose the project's models and couplers.

Distinct from the top-level ``benchmarks/`` directory, which holds the protocol,
manifest and written reports. This package holds the code those reports are
produced by.
"""
