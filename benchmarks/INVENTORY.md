<!-- GENERATED by scripts/generate_benchmark_inventory.py from benchmarks/inventory.yaml. Do not edit. -->

# Benchmark artifact inventory — CHE-130 (M0.5.1)

Every artifact under `benchmarks`, `src/agent`, `src/verification` classified into exactly one bucket, **artifact by artifact rather than directory by directory**. Files carrying more than one verdict appear once per part.

| bucket | test | action | rows |
| -- | -- | -- | -- |
| **A** | would this still be true and useful if no agent and no benchmark task existed? | preserve; name the destination | 90 |
| **B** | is this a specific physical setup whose result is worth freezing? | a positive justification is required | 3 |
| **C** | does this exist only to serve the old evaluation design? | delete | 11 |

Nothing is deleted by this inventory. Deletion is CHE-133 (M0.5.4), and the `L2-PSF-01` runner goes later still — it is the only way to run that case until the executor and family runner exist.

Coverage is enforced by `tests/test_benchmark_inventory.py`: it enumerates the scope directories and fails if a file is present in the tree and absent from this table, or if a row matches nothing. An inventory that silently omits a file is how evidence gets deleted.

## Bucket A — reusable scientific infrastructure or evidence

| artifact | justification | destination in the new architecture |
| -- | -- | -- |
| `benchmarks/agents/README.md`<br>*the A1 exclusion table* | Nine candidate benchmarks rejected with a MEASURED reason: the unseedable numba BSDF (two identical calls differ by ~1%), the VTK headless hang, the unreachable Thorlabs .zmx/.seq artifacts (1313-byte HTML page), the jax_enable_x64 exclusion, and the two Adam-state traps plus t35 rejected ONLY for runtime. Recording why a candidate is not a benchmark is expensive knowledge and the runtime rejections are the shortlist for a cheaper V2. | B0 family metadata -- the excluded-candidate register, with the two Adam-state traps and t35 carried as B0 candidates pending a cheap surrogate. |
| `benchmarks/agents/README.md`<br>*the five design decisions and the failure taxonomy table* | Context policy as a property of the task, three declared trials reported as a rate, void-vs-failed, the 8-code taxonomy, and first-stage-that-failed grading. These are harness design decisions with reasons; they survive the task set they were first written for. | Agent-evaluation harness documentation (M9). Not verifier material -- these grade an agent, not physics. |
| `benchmarks/manifest.yaml`<br>*the L2-PSF-01 entry's note: and gate_disposition: prose* | The frozen 1.0e-3 fft_oracle_intensity_relative_l2 gate is UNMET at 2.2e-3-2.5e-3 on 787,969 rays while a synthetic aberration-free bundle reaches 4.07e-4; and the rule that it must not be closed against another Optiland PSF route, because FFTPSF and HuygensPSF share one Wavefront/OPD front end and are not two oracles (PB7/CHE-58 finding F2). | B3-PSF-SINGLET: the tolerance with its basis, gate_status NOT_MET, and the oracle-independence rule as a structural constraint rather than prose. |
| `benchmarks/manifest.yaml`<br>*characterizations: block (DEMO3-RW-P)* | Already the labelling discipline this revision asks for: validation_target false, a why_not saying the paper states no conventional reference exists, what_it_does_support, and a cost_ceiling with its 2.9x model spread. It must be migrated, not rewritten from scratch. | B4-DEMO3 family metadata, verbatim where the field maps. |
| `benchmarks/manifest.yaml`<br>*m1_exit_report / m1_independence_check pointers* | Provenance pointers into the archived gen1 suite and its independence check. They are the milestone record for benchmarks that no longer run. | Family provenance_rule references for B1 ray/wave families. |
| `benchmarks/physics/L2-PSF-01/evaluate.py` | The metric definitions -- fft_oracle_intensity_relative_l2 and the rest -- exist nowhere else in executable form. | src/verification/metrics.py (M2.4), as canonical single definitions with a documented blind spot each; referenced by B3-PSF-SINGLET's metrics. |
| `benchmarks/physics/L2-PSF-01/tolerances.yaml` | Tolerance VALUES together with their derivations. A tolerance without its basis cannot be re-justified later and cannot be checked for may_gate eligibility. | B3-PSF-SINGLET tolerances, each as (threshold, basis, may_gate). Bases that are recorded measurements rather than independent derivations become may_gate false. |
| `benchmarks/physics/L2-PSF-01/README.md` | The residual analysis behind the unmet gate, including the split between attributed and unattributed residual and the negative controls -- one of which (inverted quadrature weight) fires backwards. | B3-PSF-SINGLET negative_controls, and the evidence trail CHE-117 must either close or re-scope. |
| `benchmarks/protocols/protocol.yaml` | The M1 baseline contract: declared tolerances, sampling, device and dtype for the ray and wave node baselines. | B1 ray/wave family execution_policy and tolerance bases. Moves to protocols/archive/ only once M1 expresses its content executably. |
| `benchmarks/protocols/coupler_protocol.yaml` | Defines the four stochastic evidence kinds -- exactness limit, unbiasedness, convergence exponent, variance characterization -- that StochasticEvidence already mirrors, plus the coupler tolerance derivations. | B2-W2R-STOCH stochastic_policy; B2 tolerance bases. |
| `benchmarks/protocols/slice_protocol.yaml` | 1785 lines of frozen M3 contract: the per-axis Nyquist derivation, the exclusion of M_SENSOR_IDEAL and why the graph terminates at the propagated ComplexField, the sampling and validity envelope. | ValidityPredicate bases (per-axis Nyquist from marginal ray angles; declared planarity) and B3-PSF-SINGLET execution_policy. |
| `benchmarks/protocols/m1_baseline_protocol.md` | Prose contract for the M1 baselines; carries the tolerance reasoning. | B1 family question/basis text. |
| `benchmarks/protocols/m2_coupler_protocol.md` | Prose contract for the coupler benchmarks. | B2 family question/basis text. |
| `benchmarks/protocols/m3_slice_protocol.md` | Prose contract for the M3 slice, including the reference-plane decision. | B3-PSF-SINGLET family question/basis text. |
| `benchmarks/schemas/performance.schema.json` | The performance-record contract behind the M0.4 baseline harness; taxonomy- free and still the shape the cost records are written in. | B4-COST resource_cost schema. |
| `benchmarks/schemas/provenance.schema.json` | The provenance contract, including the scientific-fingerprint projection. Reproducibility is not a property of the retired task layer. | VerificationResult.provenance and family provenance_rule. |
| `benchmarks/roadmap.md`<br>*the removed-solver findings and the independent-validation standard* | FMMAX's unresolved phase/sign convention against a reflectance magnitude matched to ~1e-7; fdtdx's two failed gradient paths (jax.grad w.r.t. source wavelength returning exactly 0.0, and ConcretizationTypeError from place_objects); jax-fem's PyPI-BSD-versus-actual-GPLv3 mismatch and its unconditional petsc4py import. Plus the standard the L3 tasks were written against: independent final validation, multiple initializations, held-out perturbations, equal compute budgets, gradient checks, and a full accounting of solver calls -- which still applies to whatever replaces them. | Retired-component register (keeps living in roadmap.md), and the independence standard becomes the oracle-independence rules the family schema enforces structurally. |
| `benchmarks/roadmap.md`<br>*the C_FIELD_TO_PSF paragraph* | Records that C_FIELD_TO_PSF was retired on a definitional argument -- extracting \|U\|^2 from a terminal field is a measurement, not a cross-representation handoff -- and names the test that pins it. Nothing in this migration reopens it. | Unchanged in place; tests/test_graph_validation.py::test_field_to_psf_is_not_a_registered_coupler stays as its executable form. |
| `benchmarks/roadmap_source_catalog.yaml` | Source catalog for the roadmap's claims -- which paper or probe each retired component's finding came from. | Retired-component register provenance. |
| `benchmarks/validation/coverage_matrix.md` | Generated from src/verification/claim_ledger.py by scripts/generate_validation_coverage.py. A view, not a second truth. | Regenerated as a view over the family registry once CLAIMS becomes a projection of it (M0.5.2). |
| `benchmarks/validation/gap_list.md` | Generated view of GAPS with the ranking criterion. The gap list is what M1/M2/M4 owe, so it is the work list for the families being authored. | Same generator, sourced from the family registry. |
| `benchmarks/validation/open_gates.md` | Generated view of the claims whose gate_status is NOT_MET. | Same generator, sourced from the family registry. |
| `benchmarks/validation/knowledge_pack_audit.md` | Records which knowledge packs are complete against KNOWLEDGE_PACK_REQUIRED_FILES; the two missing coupler packs (C_PATCH_WFT, C_PLANAR_DOE_STEP) are M2.3 deliverables. | Same generator; the two gaps are owned by CHE-112 (M2.3). |
| `benchmarks/reports/README.md` | States that reports are historical and their numbers stand. | Unchanged. |
| `benchmarks/reports/2026-08/ray_and_wave_baselines.md` | The M1 exit report for the archived gen1 ray and wave baselines. | B1 family provenance; the closed forms it verified move into families. |
| `benchmarks/reports/2026-08/coupler_characterization.md` | Coupler characterization evidence, including the deterministic-limit results. | B2-R2W-EXACT and B2-W2R-STOCH evidence. |
| `benchmarks/reports/2026-08/ray_to_wave_slice.md` | The M3 slice measurement record. | B3-PSF-SINGLET evidence. |
| `benchmarks/reports/2026-08/ray_to_wave_slice_exit.md` | The M3 exit report; carries the unmet-gate disposition. | B3-PSF-SINGLET evidence and CHE-117's starting point. |
| `benchmarks/reports/2026-08/sensor_handoff_convergence.md` | The corrected sensor-side handoff study -- the residual attribution that found the dominant term is per-ray quadrature weighting, not a kernel defect. | B3-PSF-SINGLET residual attribution; B2-R2W-ROUTE error budget. |
| `benchmarks/reports/2026-08/slice_cleanup_disposition.md` | Item-by-item disposition of the M3 cleanup, including item 3 -- the carried open gate and the prohibition on closing it against another Optiland route. | B3-PSF-SINGLET tolerance basis and oracle-independence constraint. |
| `benchmarks/reports/2026-08/cooke_triplet_psf_routes.md` | Three diffraction routes on one system. The evidence behind the rule that FFTPSF and HuygensPSF are one oracle, not two -- which is exactly the CROSS_ROUTE case the schema must refuse to let gate. | B3-DUALROUTE, category B4-constrained oracle kind CROSS_ROUTE. |
| `benchmarks/reports/2026-08/kspace_ray_to_wave.md` | The k-space reconstruction route's characterization. | B2-R2W-ROUTE representation parameter and its error budget. |
| `benchmarks/reports/2026-08/metalens_bridge.md` | The metalens bridge study. | B2-EQUIV evidence. |
| `benchmarks/reports/2026-08/demo3_estimator_variance.md` | CHE-120's variance study: the extrapolation basis for the 1.49e9-ray NCC 0.9 cost ceiling and its 2.9x model spread. Ensemble evidence, not one seed. | B4-DEMO3 characterization and B2-W2R-STOCH variance evidence. |
| `benchmarks/reports/2026-08/optiland_trace_profile.md` | CHE-118's trace profile -- 48% of demo3, 95% of it in one hash. | B4-COST. |
| `benchmarks/reports/2026-08/patch_emitter_profile.md` | CHE-119's emitter profile -- 72% of demo3 on one core. | B4-COST. |
| `benchmarks/reports/2026-08/performance_baselines.md` | The M0.4 performance-baseline report and its reproducibility rules. | B4-COST. |
| `benchmarks/reports/2026-08/agent_benchmark_v1.md`<br>*the harness validation (taxonomy reachability, void-vs-failed, trial arithmetic)* | Establishes that the grading is sound by running every negative participant and showing each of the 8 codes fires. That result is about the harness, not about the six tasks. | Agent-evaluation harness record (M9). |
| `benchmarks/perf/run_baselines.py` | The reproducible performance-baseline harness: stage decomposition, the estimate-vs-actual accounting, and the environment fingerprint. Cost is a property of a workload, not of the retired task layer. | B4-COST family runner; ExecutionRecord.resource_cost (M3.1). |
| `benchmarks/perf/profile_optiland_trace.py` | CHE-118's trace decomposition probe. | B4-COST. |
| `benchmarks/perf/profile_patch_emitter.py` | CHE-119's emitter decomposition and thread-sweep probe. | B4-COST. |
| `benchmarks/perf/records/*.json` | Committed cost baselines with their environment fingerprints -- the before/after arms of CHE-118 and CHE-119, the demo2/demo3 GPU baselines, the estimate-accuracy record, and the framework-overhead measurement. Expensive to regenerate and the only comparison points a regression would show up against. Records are provenance, never oracles. | B4-COST characterization records. |
| `benchmarks/probes/carrier_phase_representation.py` | Measured Chromatix complex64 field error growing as eps32*2*pi*z/lambda -- 2.5e-5 at 40 um, 6.3e-2 at 47 mm -- which is why the M3 reference singlet was scaled to 1/10. This is the measured precision loss as a NUMBER. | B0-DTYPE, and VerificationResult.device_precision_observation's measured precision loss. |
| `benchmarks/probes/cooke_triplet_psf_routes.py` | Three diffraction routes on one system. Its value is precisely that it demonstrates a CROSS_ROUTE comparison and therefore cannot gate. | B3-DUALROUTE / B4 cross-route consistency. |
| `benchmarks/probes/engine_independence.py` | Cross-engine independence check for the PSF oracles. | Oracle-independence evidence for B3 families. |
| `benchmarks/probes/first_null_grid_convergence.py` | CHE-103's grid-convergence study of the Airy first null, which showed the 0.9963 -> 1.0345 ratio move was grid sampling rather than a physics change. | B1-WAVE-AIRY convergence dimension and its ladder. |
| `benchmarks/probes/off_axis_handoff.py` | The silent-defect archetype: the declared pupil OPL carried 0.13% of the required tilt, the reconstructed wave converged 209 um off, and it converged CLEANLY (0.072 waves PV) so nothing downstream noticed. The missing term is n_object * (d0 . r_launch). | B1-RAY-OFFAXIS-OPL negative control -- omitting that term must fail off-axis, and must not be hidden by an on-axis case. |
| `benchmarks/probes/psf_oracle_verification.py` | Runs the shipping path against two oracles and reports how far each perturbation moves relative to the residual it must beat. That perturbation-versus-residual framing is the negative-control instrument. | B3-PSF-SINGLET negative_controls and oracle wiring. |
| `benchmarks/probes/pupil_to_focus_convergence.py` | Three independent discretizations with fitted trends. | B3-PSF-SINGLET convergence (dimension, ladder, fitted exponent). |
| `benchmarks/probes/quadrature_weight.py` | Attributes the dominant sensor residual to per-ray quadrature weighting (3.84e-3 uniform at 787,969 rays) and carries the DIAGNOSTIC radial-trapezoid weight whose inverted twin is the control that fires backwards. | B3-PSF-SINGLET negative_controls; the starting artifact for CHE-117. |
| `benchmarks/probes/sensor_handoff_convergence.py` | The corrected observation-side handoff study; supersedes the exit-pupil formulation and is why the reference plane is where it is. | B2-R2W-EXACT / B3-PSF-SINGLET reference-plane contract. |
| `benchmarks/probes/slice_feasibility.py` | Derives the per-axis Nyquist bound from marginal ray angles -- the numbers that make an infeasible slice detectable before it is run. | ValidityPredicate PER_AXIS_NYQUIST, with a normalized signed margin. |
| `benchmarks/probes/slice_summary_figure.py` | Figure generator for the M3 exit report. Not a measurement; every number it plots is recorded elsewhere. | Reporting utility; unchanged. |
| `benchmarks/probes/doe_step_sampler_cost.py` | Measures whether the two-stage marginal/conditional sampler is the bottleneck at the paper's largest scale, and records CUDA's category limit on a single multinomial draw. | B4-COST; C_PLANAR_DOE_STEP cost model. |
| `benchmarks/probes/data/*.npy` | The demo2/demo3 smile phase profiles -- input data reproduced from the paper, not results. Deleting them makes the demos unrunnable. | B3-DEMO2 / B4-DEMO3 instance parameters (input artifact). |
| `benchmarks/probes/data/README.md` | Provenance of the two phase profiles. | Unchanged. |
| `benchmarks/probes/precision/*.py` | The probes behind core/capabilities.py: Optiland's set_precision Literal and BackendCapabilityError on numpy, Chromatix's unconditional complex64 cast, the GPU matmul and tolerance measurements, and the gradient-precision study. These are the source of truth for what each component can actually execute. | B0-CONTRACT and B0-DTYPE families; ValidityPredicate CAPABILITY_INTERSECTION. |
| `benchmarks/probes/precision/README.md` | How the precision probes are run and what each establishes. | Unchanged. |
| `benchmarks/probes/optiland/*.py` | Optiland API and convention evidence: the import and raytrace probes, the opd_native sign and reference-plane convention probe, the off-axis OPD reference, the exit-pupil handoff, the gradient probe, the system-construction probe, and the standalone baseline. | B1-RAY-* oracles and conventions; B1-RAY-OFFAXIS-OPL's independent reference. |
| `benchmarks/probes/chromatix/*.py` | Chromatix API and propagation evidence: import, propagation, gradient, the M3 pupil-to-focus study, and the standalone baseline. | B1-WAVE-* oracles; B0-DTYPE. |
| `benchmarks/probes/gpu/*.py` | The CUDA stack revalidation and the C_PLANAR_DOE_STEP device probe -- the only executed evidence for the CUDA half of the capability table. | B0-CONTRACT device feasibility; B4-COST device arm. |
| `benchmarks/probes/ray_to_wave/coherent_handoff.py` | The coherent ray-to-wave handoff probe; the deterministic-limit instrument for the exact route. | B2-R2W-EXACT. |
| `benchmarks/probes/ray_wave/_demo_support.py` | Shared demo harness: configuration, record writing with provenance stamping, and the field/npz emission. Every demo record's reproducibility depends on it. | B3-DEMO2 / B4-DEMO3 family runner support. |
| `benchmarks/probes/ray_wave/demo2_cost_sweep.py` | Cost sweep over the demo2 configuration space. | B4-COST. |
| `benchmarks/probes/ray_wave/demo3_convergence.py` | The demo3 convergence ladder and its fitted trend. | B4-DEMO3 convergence. |
| `benchmarks/probes/ray_wave/demo3_variance.py` | CHE-120's estimator-variance instrument: ensemble statistics, the allocation ladder and its controls. Ensemble evidence, never one seed. | B4-DEMO3 stochastic evidence; B2-W2R-STOCH variance kind. |
| `benchmarks/probes/ray_wave/demo3_enumerated_reference.py` | The deterministic-limit instrument: enumerate everything the estimator would otherwise sample and it must reduce to the reference exactly. Independent of the SAMPLING, not of the kernel -- which is why it is an exactness limit and not a forward-accuracy oracle. | B2-R2W-EXACT exactness limit; B2-W2R-STOCH evidence kind 1. |
| `benchmarks/probes/ray_wave/demo3_reconstruction_equivalence.py` | Route-agreement instrument between the reconstruction routes. | B2-R2W-ROUTE / B4 cross-route consistency (cannot gate). |
| `benchmarks/probes/ray_wave/demo_figures.py` | Figure generation for the demo reports; no measurement of its own. | Reporting utility; unchanged. |
| `benchmarks/probes/records/*.json` | The M3 record set -- convergence, first-null grid convergence, off-axis handoff, PSF verification, quadrature weight -- plus the engine-independence report, the DOE sampler cost and the planar-DOE device record. Provenance for the measurements the reports cite, never oracles. | Family evidence citations; VerificationResult.provenance. |
| `benchmarks/probes/records/REGISTER.yaml` | The enrolled/unenrolled coverage mechanism for record provenance stamping -- the fix for the drift where committed records stopped reproducing and nothing failed. The pattern this inventory's own coverage test copies. | Unchanged; extended as families acquire records. |
| `benchmarks/probes/records/optiland/*.json` | Recorded Optiland probe output, including the off-axis OPD reference. | B1-RAY-* evidence citations. |
| `benchmarks/probes/records/chromatix/*.json` | Recorded Chromatix probe output. | B1-WAVE-* and B0-DTYPE evidence citations. |
| `benchmarks/probes/records/ray_to_wave/*.json` | Recorded coherent-handoff probe output. | B2-R2W-EXACT evidence citation. |
| `benchmarks/probes/records/ray_wave/demo2_*.json` | demo2 records, including the paper configuration and its k-space arm. | B3-DEMO2 evidence citations. |
| `benchmarks/probes/records/ray_wave/demo2_*.npz` | demo2 field arrays -- the artifacts the recorded metrics were computed from. | B3-DEMO2 evidence artifacts. |
| `benchmarks/probes/records/ray_wave/demo3_*.json` | The demo3 corpus: the characterization runs, the enumerated references and their shards, the convergence and pad sweeps, the route-agreement records and CHE-120's variance ladder with its controls. Expensive GPU compute; the enumerated shards in particular are the deterministic-limit reference. | B4-DEMO3 and B2-R2W-EXACT evidence citations. |
| `benchmarks/probes/records/ray_wave/demo3_*.npz` | demo3 field and position arrays, including the enumerated-position reference. | B4-DEMO3 and B2-R2W-EXACT evidence artifacts. |
| `benchmarks/probes/records/ray_wave/perf_demo*.json` | The four provenance-enrolled GPU cost records CHE-129 paid compute for; the only code-verified members of the demo corpus. | B4-COST characterization records. |
| `benchmarks/probes/records/ray_wave/perf_demo*.npz` | Field arrays for the enrolled GPU cost records. | B4-COST evidence artifacts. |
| `benchmarks/inventory.yaml` | This triage. It is the record of which artifacts carry scientific evidence and which are the retired task layer, and it is what CHE-133's deletion is reviewed against. | Retained until the migration is complete; the destinations it names become the family registry's contents. |
| `benchmarks/INVENTORY.md` | The reviewable rendering of inventory.yaml, generated by scripts/generate_benchmark_inventory.py and re-rendered in memory by the coverage test so the two cannot disagree. | Regenerated alongside inventory.yaml. |
| `src/agent/benchmark_suite.py`<br>*the harness* | Three declared context policies, the 8-code outcome taxonomy with its remedies, trial arithmetic reporting a rate over declared trials, void-vs-failed separation, the pluggable `command:` participant, and first-stage-that-failed grading. None of this is specific to the A1 tasks. | Stays in src/agent/ as the agent-evaluation harness (M9), which consumes VerificationResult rather than grading physics itself. |
| `src/agent/benchmark_suite.py`<br>*the closed forms inside the six task definitions* | Five analytic oracles, each VERIFIED against the pinned solver before it shipped, each with a measured tolerance basis: R/(n-1) and EFL - t/n (1e-13 relative measured); t(1-1/n) (3.750048 mm real trace, 1.3e-5 from the closed form); w0 sqrt(1+(z/zR)^2) (6.040167 vs 6.039084 um, 1.8e-4); 0.61 lambda/NA (6.65 vs 6.4985 um, a 2.3% sampling limit not a physics error); z tan(theta) (+17.5017 vs +17.4977 um, 2.3e-4). Plus the two MEASURED traps: Optiland's add_layer um/nm slip returning 0.04216384 against bare glass's 0.04216456, and Chromatix's kykx meaning cycles-per-length on asm_propagate and radians-per-length on plane_wave, with the displacement opposite in sign. | Oracles of B1-RAY-EFL, B1-RAY-PLATE, B1-WAVE-GAUSS, B1-WAVE-AIRY, B1-WAVE-TILT; traps become B0-UNITS-01 and B0-UNITS-02, where contract status is `ok` and the physics is wrong. |
| `src/agent/__init__.py` | Package marker for the agent harness. | Unchanged. |
| `src/verification/claim_ledger.py` | A hand-maintained proto-schema for exactly this architecture: Oracle, OracleIndependence, GateStatus, ClaimKind, StochasticEvidence, tolerance-with-basis, and the enforced rule that a SHARES_CODE oracle cannot gate. Reuse the enums; do not fork them. | Enums move to the family schema; CLAIMS becomes a PROJECTION of the family registry so the two cannot drift (M0.5.2). |
| `src/verification/psf_oracles.py` | The independent analytic PSF oracles. Expensive to derive and the only independent deciders the composed families have. | B3-PSF-SINGLET oracle callables. |
| `src/verification/psf_measurement.py` | PSF metric measurement -- Strehl, first null, encircled energy, residual L2. | src/verification/metrics.py (M2.4), one canonical definition each. |
| `src/verification/asm_oracle.py` | The repository's own float64 ASM/RS propagator (O2). Genuinely useful characterization evidence that must NEVER decide a gate -- L2-PSF-01 once set a negative-control floor from an O2 comparison and had to retire it as circular. | Oracle kind with independence SHARES_CODE, structurally barred from gating. |
| `src/verification/__init__.py` | Package marker. | Unchanged. |

## Bucket B — candidate canonical case

| artifact | justification | destination in the new architecture |
| -- | -- | -- |
| `benchmarks/physics/L2-PSF-01/run_benchmark.py`<br>*the M3-SINGLET-REF prescription and its graph construction* | POSITIVE: this is the only composed ray-to-wave case in the repository with an independent analytic decider, and it is the case that carries the project's one open unmet gate. Freezing it keeps the residual investigation (CHE-117) anchored to a fixed physical setup, so a change in the number is attributable to code rather than to a moved prescription. Its scientific fingerprint is the regression signal for the whole ray-to-wave path. | B3-PSF-SINGLET-01, canonical instance, origin CANONICAL. |
| `benchmarks/probes/ray_wave/demo2_hologram.py` | POSITIVE: demo2 is the composed ray-wave case that DOES have an independent decider -- the full-aperture patch validated against an independent float64 ASM at 7.1e-13 -- so freezing it gives B3 a second decidable composed instance beside the singlet, on a different coupler route. | B3-DEMO2-01 canonical instance. |
| `benchmarks/probes/ray_wave/demo3_hologram_lens.py` | POSITIVE: the paper's Fig 5c system is the repository's only case at a scale where the composition has no reference of its own, which is exactly what a characterization family must be anchored to. Freezing it keeps B4-DEMO3's cost and variance numbers comparable across the optimization work (CHE-118/119/120) rather than drifting with the configuration. | B4-DEMO3 canonical instance -- characterization only, validation_target false, no gating tolerance. |

## Bucket C — obsolete task layer

| artifact | why it goes |
| -- | -- |
| `benchmarks/agents/prompts/A1-*.md` | The old agent-facing task set. Each prompt names one library implicitly and has one right answer, so it grades tool use rather than modelling, and none was ever run against a real agent. |
| `benchmarks/agents/expected/A1-*.json` | Recorded expectations for the six A1 tasks. Explicitly a regression signal and not the oracle (the oracle is the closed form in the task definition), so nothing scientific is lost with the task. |
| `benchmarks/agents/README.md`<br>*the six-task table and "Adding a benchmark"* | Documents the A1-* task set and the procedure for extending it in the retired id space. |
| `benchmarks/agents/test_agent_suite.py` | The opt-in graded run over the six A1 tasks. Its harness-level assertions are duplicated in tests/test_agent_benchmark.py, which is in the default gate and stays; what is unique to this file is the A1 task set. **Verify before deletion that no assertion here is unique to this file. Owner CHE-133.** |
| `benchmarks/manifest.yaml`<br>*levels: block (L2-PSF-01 task entry)* | The L1/L2/L3 "levels" taxonomy is the old design, replaced by the family registry plus benchmarks/instances/. |
| `benchmarks/physics/L2-PSF-01/run_benchmark.py`<br>*the task wrapper (CLI, result.json emission, protocol plumbing)* | Serves the old evaluation design. Replaced by GraphExecutor (M3.1) plus the family runner. **NOT deleted in M0.5.4. It is the only way to run this case until the executor and family runner exist; deletion is owned by M4 (CHE-115/CHE-116).** |
| `benchmarks/schemas/result.schema.json` | Hard-codes benchmark_id enum[L1-RAY-01, L1-WAVE-01, L2-COUPLER-01] and protocol_id enums -- the retired taxonomy baked into a schema. Replaced by the VerificationResult schema (M0.5.3). |
| `benchmarks/roadmap.md`<br>*the ten L1/L2/L3 planned-benchmark rows* | Task definitions in a retired taxonomy. Four name model ids (M_TMM_JAXLAYERLUMOS, M_PC_LEGUME and the two dangling L1 entries) that never existed anywhere in this repository. |
| `benchmarks/reports/2026-08/agent_benchmark_v1.md`<br>*the per-task A1 results* | Results for the six retired A1 tasks against the reference participant. |
| `benchmarks/README.md` | Describes the per-task `benchmarks/levelN/<task_id>/` layout -- task.md, public_config.yaml, oracle_graph.yaml, cases/public, cases/hidden -- which is the retired design, and points at a `level2/` path that no longer exists. Rewritten rather than deleted; see pending_edits. |
| `src/agent/benchmark_suite.py`<br>*the six _reference_A1-* implementations and AgentTask entries* | The task definitions in the retired A1 id space. |

## Edits this triage requires but does not make

| file | section | required change | owner | why not here |
| -- | -- | -- | -- | -- |
| `AGENTS.md` | Current Phase | Rewrite from the retired two-class taxonomy ("single-component benchmarks" / "composed physics benchmarks") to the B0-B4 family architecture, and give "Benchmark Requirements" the family / instance / verifier vocabulary. | CHE-133 (M0.5.4) | AGENTS.md has in-flight uncommitted changes on an unrelated topic. Record the edit; land it with the deletion so the two are reviewed together. |
| `benchmarks/README.md` | whole file | The "What is here" table points at `level2/L2-PSF-01/` (a path that no longer exists; the directory is `physics/L2-PSF-01/`) and describes the per-task `benchmarks/levelN/<task_id>/` layout, which is the retired design. Replace with the family registry / `benchmarks/instances/` layout. | CHE-133 (M0.5.4) | This issue changes no behaviour and no repository-wide prose. |
