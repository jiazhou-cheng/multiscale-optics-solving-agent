"""The agent benchmark: can an agent turn a physics problem into a right answer?

One module, `benchmark_suite.py`. It replaces the `benchmarks/` package, whose
own docstring existed mainly to explain that it was not the repository-root
`benchmarks/` directory -- a name that had to be disambiguated in prose was the
wrong name.
"""
