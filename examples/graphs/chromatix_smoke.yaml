schema_version: 0.1.0
task_id: null
nodes:
  - id: wave
    model: M_WAVE_CHROMATIX
    config:
      propagation: angular_spectrum
      z_m: 1.0e-4
      refractive_index: 1.0
metadata:
  description: >
    Single-node smoke graph for the M_WAVE_CHROMATIX adapter. Exercises only
    the one implemented path (chromatix.functional.asm_propagate, scalar
    angular-spectrum propagation); see
    src/adapters/chromatix_adapter.py for the scope
    and conventions. No edges, design variables, or objectives are needed to
    validate a single node against the registry contract.
