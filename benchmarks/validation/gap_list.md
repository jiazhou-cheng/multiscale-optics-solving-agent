# Validation gap list

<!-- GENERATED by scripts/generate_validation_coverage.py from
     src/verification/claim_ledger.py. Do not edit: tests/test_claim_ledger.py
     regenerates this in memory and fails if the committed copy differs. -->

What is not established, ranked. This is the scope input for M1, M2 and M4:
each entry names the milestone that owes it, so those scopes are traceable here
rather than re-derived.

## Ranking criterion

```
Gaps are ranked by what they would let through, not by effort.

  critical -- a wrong answer could be produced and reported as correct, because
              nothing independent is checking. A missing oracle outranks a
              missing convergence study.
  high     -- either (a) an independent oracle IS checking and is failing, with
              the residual unattributed -- loud rather than silent, but a known
              wrong answer with no owner for the mechanism; or (b) a claim the
              repository makes in the registry that no executed evidence
              supports. The claim is the liability.
  medium   -- established on one path and extrapolated to another (a device, a
              dtype, a field position) without measurement; or a usability gap
              that blocks an agent from using a component correctly without
              itself making anything numerically wrong.
  low      -- a real gap whose failure mode is visible rather than silent.

Two buckets were added after a first pass ranked by importance instead of by
this criterion. A known-failing gate is NOT critical under the definition above,
because something independent is checking -- it is loudly wrong, which is the
better failure. And a missing knowledge pack is not a claim at all, so it never
fitted "high"; it blocks an agent rather than producing a wrong number.
```

## critical (6)

### `C_RAY_TO_WAVE` / convergence -- M2.1 (CHE-109)

The frozen M3 configuration samples the Airy radius with 2.44 pixels, so radius-like metrics measured on it are not converged, and the grid has never been swept as a convergence variable in the benchmark itself.

* **why critical** CHE-103 measured this and it silently invalidated a 1%-of-unity assertion that had been reading as a pass. Anything else measured on this grid at sub-pixel scale is suspect until the sweep exists.
* **blocks** first-null accuracy, airy_peak_intensity_relative attribution

### `C_RAY_TO_WAVE` / forward_accuracy -- M2.1 (CHE-109)

Off-axis, the reconstruction is 7.5x worse against the analytic Airy oracle with the production quadrature weight than without it, and on axis it is not. No explanation is established.

* **why critical** Found by CHE-103 only because the records were regenerated; no test was asserting on it. The direction is the wrong way round for a correction that is supposed to improve the quadrature.
* **blocks** any off-axis composed benchmark, the L2-PSF-01 open item

### `C_PLANAR_DOE_STEP` / convergence -- M2.3 (CHE-111)

Three of the four stochastic evidence kinds coupler_protocol.yaml requires are absent: no unbiasedness, no fitted convergence exponent, no variance characterization. Only the exactness limit exists.

* **why critical** The textbook critical case: a biased estimator at a usable ray count would be reported as correct, because the only thing checking is the enumeration limit, which by construction removes the sampling that would carry the bias. Nothing independent checks the sampled step.
* **blocks** any claim about the SAMPLED step at a usable ray count

### `C_PATCH_WFT` / convergence -- M2.3 (CHE-111)

Same as C_PLANAR_DOE_STEP: exactness limit only, three kinds missing.

* **why critical** Same as C_PLANAR_DOE_STEP: the exactness limit removes the sampling, so nothing checks the estimator anyone would actually run.
* **blocks** any claim about the SAMPLED patch estimator

### `M_WAVE_CHROMATIX` / forward_accuracy -- M1.2 (CHE-107)

No INDEPENDENT forward-accuracy oracle. The ASM is checked against our own float64 propagator built from Chromatix's own kernel_propagate, which verifies the carrier bookkeeping and cannot verify the propagation.

* **why critical** This is the circular-validation case the repository has a standing rule about. An analytic diffraction case -- a Gaussian beam's closed-form propagation, or a knife edge -- would settle it and does not exist.
* **blocks** any gate that would rest on Chromatix being right

### `M_RAY_OPTILAND` / forward_accuracy -- M1.1 (CHE-106)

No analytic oracle covers a CURVED refracting surface. Flat refraction is covered -- the exactness limit traces a plane-parallel n=1.5 slab against a layered plane-wave oracle at 1e-11, and the canonical-prescription tests check the grating equation and the even-asphere sag series against closed forms. What has no closed-form check is the traced OPD of a lens.

* **why critical** Every composed benchmark starts from a traced OPD map of a CURVED system, and what validates it today is Optiland's own Wavefront agreeing with our reference-sphere fit -- two readings of one trace. A paraxial-limit or Seidel cross-check on the singlet would be independent and does not exist.
* **blocks** every claim that the traced OPD of a real lens is right

## high (2)

### `C_RAY_TO_WAVE` / forward_accuracy -- M4.2 (CHE-117)

The only forward-accuracy claim that survives on a REAL traced aberrated system fails its gate at 2.21e-3 against 1.0e-3, and the residual is not decomposed. The synthetic analytic claims pass and do not transfer.

* **why high** high, not critical: an independent oracle IS checking and IS failing, loudly, at 2.2x. What is missing is an attribution for the residual, not a check. Ranked above the convergence gaps because a composed benchmark cannot be built on an edge whose error nobody can decompose.
* **blocks** L2-PSF-01, every composed benchmark M4 builds on this edge

### `M_WAVE_CHROMATIX` / convention -- M3.1 (CHE-113)

jax_enable_x64 is process-global and nothing checks it at the boundary that depends on it, so the precision of a committed measurement depends on import order.

* **why high** CHE-103 established this caused a real, undetected change in committed evidence. It is CHE-102's rule -- check process-global solver state at the boundary, do not set it earlier and hope -- applied to the wave solver.
* **blocks** reproducibility of any wave-side record, M3's executor

## medium (2)

### `C_PATCH_WFT` / convention -- M2.3 (CHE-111)

No knowledge pack at all: no card, no conventions, no failure guide, no theory. It is now the ONLY coupler with a graph node and no pack -- C_PLANAR_DOE_STEP's was written and this one was deliberately not.

* **why medium** A usability gap, not a correctness one: nothing here makes a number wrong. It does mean an agent asked to build a graph through this node has nothing to read about its conventions, and the conventions are exactly what a coupler gets wrong. Deferred rather than forgotten: this coupler's estimator contract -- the centre density, the unbiasedness weights, the measured variance reduction -- is being rewritten by CHE-120, so a pack written against the current state would be wrong on landing. Write it once that lands.
* **blocks** agent use of this coupler, M6's planner, M3.2's discovery API

### `C_RAY_TO_WAVE` / device_parity -- M0.4 (CHE-105) for measurement, M1.3 (CHE-108) for the gate

No CUDA execution of the composed ray->wave path is covered by the default gate.

* **why medium** Declared in the capability table and extrapolated from CPU.
* **blocks** any GPU performance or accuracy claim on this edge

## low (1)

### `C_RAY_TO_WAVE` / gradient -- deferred beyond this project phase

No verified derivative across the ray->wave boundary; forward_only.

* **why low** The failure mode is visible rather than silent: the registry says verified: false and the adapter refuses a gradient request before running.
* **blocks** inverse design through this edge

## Owners, rolled up

| Milestone | Gaps |
| -- | -- |
| M0.4 (CHE-105) for measurement, M1.3 (CHE-108) for the gate | 1 (worst: medium) |
| M1.1 (CHE-106) | 1 (worst: critical) |
| M1.2 (CHE-107) | 1 (worst: critical) |
| M2.1 (CHE-109) | 2 (worst: critical) |
| M2.3 (CHE-111) | 3 (worst: critical) |
| M3.1 (CHE-113) | 1 (worst: high) |
| M4.2 (CHE-117) | 1 (worst: high) |
| deferred beyond this project phase | 1 (worst: low) |
